define('localization/saleslogix/<%= culture %>', ['localization/<%= culture %>', 'Mobile/SalesLogix/ApplicationModule'], function() {
<% localized.each do |name, object| %>

localize("<%= name %>", <%= JSON.pretty_generate(object) %>);
<% end %>
});